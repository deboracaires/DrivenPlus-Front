import styled from 'styled-components';

const Title = styled.h1 `
  color: #fff;
  font-family: 'Roboto';
  font-style: normal;
  font-weight: 700;
  font-size: 32px;
  line-height: 38px;
  margin-bottom: 24px;
`;

export default Title;